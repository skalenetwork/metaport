import { useState } from "react";

import { Canvas, Meta, Story, ArgsTable } from "@storybook/blocks";

import Grid from "@mui/material/Grid";
import TextField from "@mui/material/TextField";
import Button from "@mui/material/Button";

import { internalEvents } from "../../core/events";
import * as WidgetStories from "./Widget.stories";

import { TransferRequestEditor } from "../WidgetUI/StorybookHelper";

# Functional Metaport Demo

<Grid container spacing={2}>
  <Grid className="fl-centered" item md={7} sm={12}>
    ## Sandbox mode
    You can try functional Metaport demo in a Sandbox mode here.
    <Meta of={WidgetStories} />
    <Canvas of={WidgetStories.WidgetDemo} />
  </Grid>
  <Grid className="fl-centered" item md={5} sm={12}>

## Transfer request mode
  You can try functional Metaport demo in a Transfer request mode here. 
  {TransferRequestEditor()}
  </Grid>
</Grid>
